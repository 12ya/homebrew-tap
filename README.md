# Homebrew tap for V12 Code

Install the latest stable macOS release:

```bash
brew install --cask 12ya/tap/v12-code
```

The `v12-code` cask is generated from the latest stable release in
[`12ya/t3code`](https://github.com/12ya/t3code). An hourly GitHub Actions workflow keeps the
version and checksums current.

